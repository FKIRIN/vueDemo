const getters = {
  // username: state => state.app.username,
  // className: state => state.app.className,
  // userRouter: state => state.app.userRouter,
  // list: state => state.products.list,
}

export default getters
